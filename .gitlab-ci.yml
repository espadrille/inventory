stages:
  - lint

lint:
  stage: lint
  image: python:3.11-slim
  tags:
    - docker
    - python
  script:
    - pip install --upgrade pip
    - pip install pylint
    - pip install boto3
    - pylint --rcfile=.pylintrc $(find . -name "*.py")
